import Foundation

enum CardSize {
    case poster   // 2:3 portrait — movies
    case wide     // 16:9 landscape — continue watching
    case thumb    // 16:9 landscape — TV shows (uses Thumb or Backdrop image)
}
